"""The lineage graph skeleton payload ({nodes, edges, levels}) for the UI.

Lightweight ids/step_type/generation/edges for layout; full metadata is
fetched on demand (live server) or inlined once (static export) (AD11).

See REBUILD_BLUEPRINT.md section 5.3. Arrives in Phase 7 (issue #18).
"""
