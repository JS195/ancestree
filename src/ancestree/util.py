"""Small shared helpers: JSON coercion and ISO time parsing/formatting.

``to_jsonable`` duck-types numpy/pandas values to JSON-native Python (both
stay optional dependencies).

See REBUILD_BLUEPRINT.md section 5.3. Arrives in Phase 2 (issue #13).
"""
