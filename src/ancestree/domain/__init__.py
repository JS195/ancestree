"""Domain layer — the vocabulary of lineage. Pure logic, no I/O.

Node record + recording handle (node.py), the metadata envelope
(metadata.py), transition rules (rules.py), content identity
(fingerprint.py) and provenance capture (provenance.py).

See REBUILD_BLUEPRINT.md section 5.3.
"""
