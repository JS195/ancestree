"""Persistence layer — everything SQLite.

ConnectionManager (connection.py), the canonical schema (schema.py),
MetadataStore (metadata_store.py) and ChunkStore (chunk_store.py).

See REBUILD_BLUEPRINT.md section 5.3.
"""
