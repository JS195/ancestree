# Legacy 0.1.x notebooks

These notebooks target the old file-based 0.1.x API and will not run against 0.2.0. Keeping them on record - they document the behaviour and benchmarks the rebuild was measured against. The current quickstart lives one directory up: sql_backend_quickstart.ipynb.
